{
  "name": "indra-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "INDRA AI — Enterprise Institutional Knowledge Intelligence Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "packageManager": "pnpm@9.0.0"
}
